react 4layers of architecture

UI
=> components
=> pages

hook => manage state and api calls
=> custom hooks

state => data store
=> auth.context.jsx
=> ai.context.jsx

api => for communication with backend
=> services
    =>auth.api.js